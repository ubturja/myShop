FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (use npm install if lockfile missing)
RUN npm install --production=false

# Copy application files
COPY . .

# Expose port
EXPOSE 3001

# Start the service
CMD ["npm", "run", "dev"]
